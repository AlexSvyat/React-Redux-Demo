// This is Spinner React Component to
// be used to show a spinner during async calls
// It also uses pure CSS spinner implementation, located in Spinner.css

import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return <div className="loader">Loading...</div>;
};

export default Spinner;
